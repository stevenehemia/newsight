# Environment variables Newsight reads. Set the real values in your environment (or your host's
# settings, e.g. Render), never in this file. A missing key switches that source off; the app still
# starts and the other sources keep working.
NYT_API_KEY=
GUARDIAN_API_KEY=
